export {};

// Concept 23: Ambient declarations (declare)
// Use to describe existing global types without an implementation.

declare const GLOBAL_VERSION: string;

const versionLabel = `v${GLOBAL_VERSION}`;
